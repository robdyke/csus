<%= form_for(@trust) do |f| %>
  <% if @trust.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trust.errors.count, "error") %> prohibited this trust from being saved:</h2>

      <ul>
      <% @trust.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trust_name %><br>
    <%= f.text_area :trust_name %>
  </div>

  <div class="field">
    <%= f.label :nhs_code %><br>
    <%= f.text_area :nhs_code %>
  </div>

  <div class="field">
    <%= f.label :trust_post_code %><br>
    <%= f.text_area :trust_post_code %>
  </div>

  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_area :latitude %>
  </div>

  <div class="field">
    <%= f.label :longitude %><br>
    <%= f.text_area :longitude %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
